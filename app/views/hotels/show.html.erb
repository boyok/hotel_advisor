<% provide(:title, @hotel.title) %>
<div class="row">
  	<aside class="span4">
   		<section>
   			<h1> <%= @hotel.title %> </h1>
   		</section>
  	</aside>
	<div class="span8">
		<ol class="microposts">
			<%= @hotel.title %> 
		</ol>
	</div>
</div>